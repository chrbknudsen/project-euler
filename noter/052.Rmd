---
title: "Euler 52"
format: html
date: '2018-07-05'
---

The number 125874 and its double 251748 contains the same digits, but in a different
order.

Identify the smallest integer x, such that 2x, 3x, 4x, 5x and 6x contains the same
digits.

First of all, there is an upper limit on x:

142857*6


```{r}
142857*6
```

Det kan vi bruge til at sætte øvre grænse. 
når ceiling(log10(x*6)) > ceiling(log10(x)) så er det tid at 
starte forfra med en ny serie tal.

```{r}
not_found <- TRUE
i <- 1
while(not_found){
   
}
```

```{r}
source("solutions/helpers.R")
n_digits(99999)
```

```{r}
floor(log10(99999))
```


```{r}
9/6
```